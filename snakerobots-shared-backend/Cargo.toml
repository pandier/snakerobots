[package]
name = "snakerobots-shared-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
snakerobots-shared = { path = "../snakerobots-shared" }

redis = { version = "1.0", features = ["tokio-comp"] }

serde = "1.0"
serde_json = "1.0"

thiserror = "2.0"
