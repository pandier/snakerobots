[package]
name = "snakerobots-godot"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = "0.4.5"
tokio = { version = "1.50.0", features = ["rt", "sync"] }
snakerobots-logic = { path = "../snakerobots-logic" }
rand = "0.10"
