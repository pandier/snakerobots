[package]
name = "rowplus-derive"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
rowplus = { path = "../rowplus" }
syn = { version = "2.0", features = ["full"] }
quote = "1.0"

[dev-dependencies]
sqlx = { version = "0.8", features = ["postgres"] }
