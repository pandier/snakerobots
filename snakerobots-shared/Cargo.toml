[package]
name = "snakerobots-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
